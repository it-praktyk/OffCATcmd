install:
  - cinst pester

build: false

test_script:
  - ps: $res = Invoke-Pester -Path ".\Tests" -Tags Help -OutputFormat NUnitXml -OutputFile TestsResults.xml -PassThru
  - ps: (New-Object 'System.Net.WebClient').UploadFile("https://ci.appveyor.com/api/testresults/nunit/$($env:APPVEYOR_JOB_ID)", (Resolve-Path .\TestsResults.xml))
  - ps: Install-PackageProvider NuGet -MinimumVersion '2.8.5.201' -Force ; Import-PackageProvider NuGet -MinimumVersion '2.8.5.201' -Force ; Install-Module -Name platyPs -MinimumVersion 0.5.0 -Repository PSGallery -Force
  - ps: if ($res.FailedCount -gt 0) { throw "$($res.FailedCount) tests failed."} Else { Update-MarkdownHelpModule -Path .\Docs\ -LogPath .\Docs\DocsLogs.txt -LogAppend  }
  
  
artifacts:
  - path: Docs